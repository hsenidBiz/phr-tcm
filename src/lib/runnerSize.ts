/** The runner window's size, in CSS pixels. Its own module, with no
 * imports, so the help site (docs-site/) can capture and frame the runner
 * at exactly this size without pulling in the Tauri window API. */
export const RUNNER_WIDTH = 460;
export const RUNNER_HEIGHT = 720;
